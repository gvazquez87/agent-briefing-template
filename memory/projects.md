# Projects

<!-- Per-project facts an agent should know cold: what each repo is, where it
     lives on which machine, conventions that are easy to get wrong. Keep one
     `## project-name` section per project. Same append-only rules as
     preferences.md. -->

## example-project
- TODO: replace with your first project (what it is, where it lives, gotchas)
